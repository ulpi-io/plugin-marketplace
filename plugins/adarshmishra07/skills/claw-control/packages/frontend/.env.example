# Frontend Configuration
# ======================

# Backend API URL - Two options:

# Option 1: Build-time (for local dev or pre-built images)
VITE_API_URL=http://localhost:3001

# Option 2: Runtime (for Railway/Docker - injected at container startup)
# API_URL=https://your-backend-service.up.railway.app

# Railway template uses variable references:
# API_URL=https://${{@claw-control/backend.RAILWAY_PUBLIC_DOMAIN}}
