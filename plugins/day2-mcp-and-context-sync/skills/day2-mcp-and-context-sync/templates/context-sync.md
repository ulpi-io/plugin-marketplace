---
name: my-context-sync
description: 나의 컨텍스트 싱크. 여러 소스에서 정보를 수집하고 하나의 문서로 정리한다. "싱크", "sync", "정보 수집" 요청에 사용.
triggers:
  - "싱크"
  - "sync"
  - "정보 수집"
  - "컨텍스트 싱크"
---

# My Context Sync

흩어진 정보를 한곳에 모아 정리하는 스킬.

4개 소스에서 최근 정보를 수집하고, 하나의 마크다운 문서로 통합한다.

## 소스 정의

<!-- ============================================ -->
<!-- 아래 4개 소스는 Block 6~9에서 하나씩 채워집니다 -->
<!-- 각 블록에서 도구를 연결한 뒤 STUB을 실제 내용으로 교체합니다 -->
<!-- ============================================ -->

### 소스 1: Slack

<!-- 🔧 Block 6에서 채움 (연결 방법: Connector) -->

| 항목 | 값 |
|------|-----|
| 연결 방법 | `[STUB - Block 6에서 Connector로 연결]` |
| 수집 범위 | 최근 7일 |

수집할 채널 목록:
```yaml
channels:
  - name: "[STUB - Block 6에서 설정]"
  - name: "[STUB - Block 6에서 설정]"
```

수집 방법:
```
[STUB - Block 6에서 Connector 연결 후 작성]
```

추출할 정보:
- 중요 공지사항
- 의사결정 사항 ("확정", "결정", "합의" 키워드)
- 나에게 멘션된 메시지
- 답장이 필요한 질문

### 소스 2: Notion

<!-- 🔧 Block 7에서 채움 (연결 방법: claude mcp add) -->

| 항목 | 값 |
|------|-----|
| 연결 방법 | `[STUB - Block 7에서 claude mcp add로 연결]` |
| 수집 범위 | 지정된 데이터베이스 |

```yaml
databases:
  - name: "[STUB - Block 7에서 설정]"
    id: "[STUB]"
```

수집 방법:
```
[STUB - Block 7에서 MCP 서버 연결 후 작성]
```

추출할 정보:
- 진행 중인 태스크
- 기한이 임박한 항목
- 최근 업데이트된 페이지

### 소스 3: Linear

<!-- 🔧 Block 8에서 채움 (연결 방법: Official Plugin) -->

| 항목 | 값 |
|------|-----|
| 연결 방법 | `[STUB - Block 8에서 /plugin install로 연결]` |
| 수집 범위 | 나에게 할당된 이슈 |

수집 방법:
```
[STUB - Block 8에서 Plugin 설치 후 작성]
```

추출할 정보:
- 진행 중인 이슈
- 이번 주 마감 이슈
- 최근 댓글/업데이트

### 소스 4: Google Calendar / Gmail

<!-- 🔧 Block 9에서 채움 (연결 방법: Plugin 구조 분석 + 설치) -->

| 항목 | 값 |
|------|-----|
| 연결 방법 | `[STUB - Block 9에서 커뮤니티 Plugin으로 연결]` |
| 수집 범위 | 오늘 ~ 7일 후 (Calendar) / 최근 7일 (Gmail) |

수집 방법:
```
[STUB - Block 9에서 Plugin 설치 후 작성]
```

추출할 정보:
- 오늘의 일정 + 이번 주 주요 미팅
- 안 읽은 이메일 요약
- 회신이 필요한 이메일
- 준비가 필요한 미팅

## 실행 흐름

<!-- 🔧 Block 10에서 완성 -->

이 스킬이 트리거되면 아래 순서로 실행한다.

### 1단계: 병렬 수집

4개 소스를 **동시에** 수집한다. 서로 의존성이 없으므로 병렬 실행이 가능하다.

```
수집 시작
  ├── [소스 1] Slack 채널 메시지 수집        ─┐
  ├── [소스 2] Notion 태스크 수집            ├── 병렬 실행
  ├── [소스 3] Linear 이슈 수집             │
  └── [소스 4] Google Calendar/Gmail 수집   ─┘
수집 완료
```

각 소스 수집은 subagent(Agent 도구)로 실행한다:

```
[STUB - Block 10에서 소스별 Agent 호출 구체화]
```

### 2단계: 결과 통합

수집된 정보를 하나의 문서로 합친다.

통합 규칙:
- 소스별 섹션으로 구분
- 각 섹션에서 "하이라이트" (중요 항목 3개 이내)를 선별
- 액션 아이템을 문서 하단에 모아서 정리
- 수집 실패한 소스는 "수집 실패" 표시와 함께 사유 기록

### 3단계: 문서 저장

결과 파일을 저장한다.

```
저장 위치: sync/YYYY-MM-DD-context-sync.md
```

### 4단계: 리포트

```
[STUB - Block 10에서 리포트 형식 구체화]
```

## 출력 포맷

<!-- 🔧 Block 10에서 완성 -->

```
[STUB - Block 10에서 출력 형식 선택 후 작성]

옵션:
1. Markdown 파일 (기본) -- sync/YYYY-MM-DD-context-sync.md에 저장
2. + Slack 메시지 -- 지정 채널에 요약 발송
3. + Notion 페이지 -- 지정 DB에 기록
```

## 커스터마이징 가이드

### 소스 추가하기

새로운 소스를 추가하려면 "소스 정의" 섹션에 같은 형식으로 추가한다:

```markdown
### 소스 5: {도구명}

| 항목 | 값 |
|------|-----|
| 연결 방법 | `{연결 방법}` |
| 수집 범위 | {범위} |

수집 방법:
{MCP 호출 방법 또는 스크립트 실행 방법}

추출할 정보:
- {항목 1}
- {항목 2}
```

### 소스 제거하기

사용하지 않는 소스는 해당 "소스 N" 섹션 전체를 삭제한다.
실행 흐름의 병렬 수집 부분에서도 해당 줄을 제거한다.

### 수집 주기 변경하기

기본은 수동 실행이다. 자동 실행을 원하면 CLAUDE.md에 스케줄을 추가한다:

```markdown
## 스케줄
- context-sync: 매일 09:00 실행
```
