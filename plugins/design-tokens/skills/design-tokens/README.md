# design-tokens

Generate type scales, color palettes, spacing systems, and dark mode derivations with math — not guessing. Includes WCAG contrast formulas, systematic shade generation, and production-ready CSS/Tailwind output.

## Installation

Copy the `design-tokens` folder into your agent's skills directory.

## What's Inside

- **Type scale generator** with ratio-based calculation and line-height/letter-spacing/weight rules
- **Color palette generator** with HSL-based shade scales (50–950) and semantic token mapping
- **WCAG contrast checker** with the actual luminance formula and pass/fail requirements
- **Spacing system** using base-4 grid (matches Tailwind)
- **Dark mode derivation** — systematic inversion, not manual picking
- **Multiple output formats** — CSS custom properties, Tailwind config, JSON (W3C)

## Usage Examples

### Set up a new project
> "Set up design tokens for a SaaS dashboard"

### Generate a palette
> "I need a color palette from this brand color: #6366F1"

### Create a type scale
> "Create a type scale for a blog"

## License

MIT
