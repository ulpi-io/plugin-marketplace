# java-maven-gradle Configuration
# Category: general
# Generated: 2025-12-30

skill:
  name: java-maven-gradle
  version: "1.0.0"
  category: general

settings:
  # Default settings for java-maven-gradle
  enabled: true
  log_level: info

  # Category-specific defaults
  validation:
    strict_mode: false
    auto_fix: false

  output:
    format: markdown
    include_examples: true

# Environment-specific overrides
environments:
  development:
    log_level: debug
    validation:
      strict_mode: false

  production:
    log_level: warn
    validation:
      strict_mode: true

# Integration settings
integrations:
  # Enable/disable integrations
  git: true
  linter: true
  formatter: true
