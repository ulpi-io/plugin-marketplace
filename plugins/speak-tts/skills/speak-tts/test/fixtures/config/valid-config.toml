# Valid speak CLI configuration for testing

output_dir = "~/Audio/speak-test"
model = "mlx-community/chatterbox-turbo-8bit"
temperature = 0.7
speed = 1.2
markdown_mode = "smart"
code_blocks = "skip"
daemon = true
log_level = "debug"
