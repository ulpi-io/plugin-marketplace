Searches posts matching a query. By default searches the last 7 days (GET /2/tweets/search/recent). With `--all`, searches the full archive back to 2006 (GET /2/tweets/search/all). Invoke via `node <base_directory>/x.js search "<query>" [flags]`. Output is JSON to stdout.

[!FLAGS] a) no flags — returns up to 10 results with default tweet fields (text, author_id, created_at, conversation_id, public_metrics) and expands author (name, username). b) `--all` — search the full archive instead of just the last 7 days. c) `--max-results <10-100>` — set number of results per page (up to 500 with --all). d) `--sort <recency|relevancy>` — order results. e) `--start-time <ISO8601>` — oldest timestamp (inclusive). f) `--end-time <ISO8601>` — newest timestamp (exclusive). g) `--since-id <id>` — results newer than this post ID. h) `--until-id <id>` — results older than this post ID. i) `--next-token <token>` — pagination token from a previous response. j) `--fields <comma-list>` — override default tweet fields. k) `--raw` — output the full API response envelope (data, includes, meta, errors).

[!QUERY-SYNTAX] The query uses X search operators. Examples: `from:username`, `has:media`, `-is:retweet`, `#hashtag`, `"exact phrase"`, `lang:en`. Combine with AND (space), OR, NOT (-). Max 4096 chars. Full reference: https://docs.x.com/x-api/posts/search/integrate/build-a-query.

[!OUTPUT-SHAPE] Default produces an array of tweet objects. With `--raw`, wraps into the API envelope with `data` (tweets), `includes` (expanded users), and `meta` (result_count, next_token for pagination).
